const inputText = require('./inpuText');
const button = require('./button');
const notification = require('./notify');
const dropdown = require('./dropdown');
const calendar = require('./calendar');

module.exports = {
    inputText,
    button,
    checkbox : button,
    notification,
    dropdown,
    calendar
};
