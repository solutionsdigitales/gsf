{
  "name": "GSF",
  "scripts": {
    "dev": "yarn build && node server/app.js",
    "build": "cd ./client && yarn build",
    "watch": "cd ./client && yarn serve"
  },
  "dependencies": {
    "nodemon": "^3.1.10"
  }
}
